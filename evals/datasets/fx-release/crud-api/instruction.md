Create an Express.js REST API with CRUD endpoints for a `todos` resource:

- `GET /todos`
- `POST /todos`
- `PUT /todos/:id`
- `DELETE /todos/:id`

Use TypeScript. Store todos in memory. Include tests with Vitest and Supertest. Make sure `npm test` passes.
