Create a Node.js CLI tool called `wordcount`.

The CLI must read a file path and print the number of words, lines, and characters in that file. Include a test suite using Vitest. Make sure `npm test` passes.
